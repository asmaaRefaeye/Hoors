<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hoors">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.SavedLady"></activity>
        <activity android:name=".Views.ClothsDesActivity" />
        <activity android:name=".Views.BehavioursActivity" />
        <activity android:name=".Views.DuatersActivity" />
        <activity android:name=".Views.WivesActivity" />
        <activity android:name=".Views.LadyDescriptionActivity" />
        <activity android:name=".Views.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.MainActivity" />
    </application>

</manifest>